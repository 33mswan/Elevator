package main.buttons;

// These are the buttons inside the elevator that are not floor numbers these buttons are enums because they will not change
public enum ElevatorInsideButtons {
    DOOROPEN,
    DOORClOSE,
    CALLHELP
}
