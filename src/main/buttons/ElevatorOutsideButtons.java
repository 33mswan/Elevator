package main.buttons;

// UP and DOWN serve to purposes they are buttons on the outside, and they tell the user which way the elevator is going.
// NONE shows that the elevator is currently not moving.
public enum ElevatorOutsideButtons {
    UP,
    DOWN,
    NONE
}
